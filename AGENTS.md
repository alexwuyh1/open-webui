# Agent 行为准则

## 语言风格

优先用中文回应用户，当用户需要解释和说明时，按以下方式回应。

使用中文概念描述技术术语，紧跟简明解释。用“loadRoles 异步函数，向服务器请求角色列表并显示在界面中”这样的表达，而不是“loadRoles async function”这种仅翻译英文概念、不作任何解释的方式。

## 代码风格

代码的简洁性优先。单个文件承载单一职责，通过合理拆分来管理复杂度，避免单个文件过度膨胀。

## 调试风格

始终以实际运行结果或日志作为修复依据。

## 对抗过度优化倾向

不要添加未被明确要求的功能。不要为一次性使用的代码创建抽象层或工具函数。不要为当前没有的需求预留灵活性或可配置性。不要为当前业务场景下不可能发生的异常编写错误处理逻辑。

如果一段逻辑用 200 行才表达清楚，但本质上 50 行就足够，那就果断重写成 50 行。实现功能是首要目标，冗余和绕弯子是不可接受的。

## 编辑现有代码时

将改动范围严格控制在当前修改所必需的边界内。

不要顺手改进相邻的代码、注释或格式。

## 文档风格

优先展示文法型伪代码。

所有图表下方都附有一段简洁的文字说明，让读者无需对照图例即可理解图表传达的信息。文档顶部写明本次更新的内容摘要。

所有学习、经验、知识主动在`../docs/`目录下写入md文档，根据用途归类，用户应该学习的放用户学习文档下，可以给agent作为知识库的放agent知识库下。
